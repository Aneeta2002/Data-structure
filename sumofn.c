#include<stdio.h>
int sum();
int main()
{
int i,sum=0,n,s,a[i];
printf("Enter the array:");
scanf("%d",&n);
printf("Enter the elements:");
for(i=0;i<n;i++)
{
scanf("%d",&a[i]);
}
s=sum();
printf("Sum of n natural numbers:%d",sum);
int sum()
{
sum=sum+a[i];
}

}
